Solution

initialize first element as the closest

loop through array and compare closest against other values
reassign closest to current number in array if its less than current closest

if closest is negative check if positive value of same exists in the array
reassign closest to postive value of the same if exists
else return closest

time complexity - O(N)